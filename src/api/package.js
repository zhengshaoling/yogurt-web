import request from '@/utils/request';
const auth = '/auth';

// 套餐管理-套餐列表
export function getPackageList(data) {
  return request({
    url: '',
    method: 'post',
    data
  });
}
// 套餐管理-删除套餐
export function handleDeletePackage(data) {
  return request({
    url: '',
    method: 'post',
    data
  });
}

// 套餐管理-添加套餐
export function handleAddPackage(data) {
  return request({
    url: '',
    method: 'post',
    data
  });
}
