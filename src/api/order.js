import request from '@/utils/request';

const auth = '/auth';

// 订单管理-订单列表
export function getOrderList(data) {
  return request({
    url: '',
    method: 'post',
    data
  });
}

export function handleDeleteOrder(data) {
  return request({
    url: '',
    method: 'post',
    data
  });
}
